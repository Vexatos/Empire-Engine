package dark.empire.core.commands;

public class CommandTransform
{
    //TODO use in combination with dev player's only allowing the to transform into there own creations
    //Args /Transform entityName
}
